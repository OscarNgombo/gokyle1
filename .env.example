# Copy this file to gokyle1/.env.local for local development.
# Set the backend origin only; the frontend app appends /api/v1 automatically.
# When running locally, the admin login URL is /staff/login and uses the backend bootstrap admin account.
# Current bootstrap admin docs:
#   email: gokyletours@gmail.com
#   password: password@123
VITE_API_BASE_URL=http://localhost:8000
